{
  "buildCommand": "npm --prefix frontend install && npm --prefix frontend run generate",
  "outputDirectory": "frontend/.output/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
